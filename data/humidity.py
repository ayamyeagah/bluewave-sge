import random

def hum1():
    # handle incoming data from humidity sensor
    data = round(random.uniform(50,90), 2)

    return data

def hum2():
    # handle incoming data from humidity sensor
    data = round(random.uniform(50,90), 2)

    return data